name: CI Unit

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch: {}   # 手动触发按钮

jobs:
  unit-core:
    runs-on: ubuntu-22.04
    timeout-minutes: 20

    steps:
      - name: 拉代码
        uses: actions/checkout@v4

      - name: 配置 CMake
        run: cmake -B server/build -S server

      - name: 编译后端与测试
        run: cmake --build server/build -j

      - name: 运行核心单元测试
        run: |
          cd server/build
          ctest -R "^(HttpContextTest|ThreadPoolTest|TraceSessionManagerUnitTest|SqliteTraceRepositoryTest)\\." --output-on-failure
