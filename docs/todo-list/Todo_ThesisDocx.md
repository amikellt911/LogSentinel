# Todo_ThesisDocx

- [x] 确认只以 `毕设论文初稿草稿.docx` 作为安全底稿，不再基于任何派生版继续修改
- [x] 复盘 Word 修复弹窗的根因，确认禁止整树重写 `word/document.xml`
- [x] 提取原始 `word/document.xml`，定位第 5 章结果段落的精确 XML 锚点
- [x] 基于 benchmark 产物生成符合论文口径的正文表格 XML 片段
- [x] 仅用最小字符串插入方式把表格和引用性文字放入正文第 5 章
- [x] 重新打包新 `.docx`，先做结构级检查，再交给用户在 Windows Word 实测
- [ ] 如果安全版无弹窗，再单独评估图片是否需要继续并入正文
- [x] 为第 4.1 节补“图 + 代码框 + 压缩文字”证据版内容
- [x] 为 `4.1.1 / 4.1.2 / 4.1.3` 生成稳定的 PNG 机制图
- [x] 在论文正文第 4.1 节插入代码框，并避免使用易导致兼容问题的浮动文本框
- [x] 产出新的第 4.1 节修订版 `.docx`，并先做结构级自检
- [x] 扩写第 4.1 节三个代码框，让每个代码框形成“关键分支闭环”
- [x] 为第 4.2 节补“图 + 代码框 + 压缩文字”证据版内容
- [x] 为 `4.2.1 / 4.2.2 / 4.2.3` 生成稳定的 PNG 机制图
- [x] 产出新的第 4.1-4.2 节修订版 `.docx`，并先做结构级自检
- [x] 为第 4.3 节补“图 + 代码框 + 压缩文字”证据版内容
- [x] 为 `4.3.1 / 4.3.2 / 4.3.3` 生成稳定的 PNG 机制图
- [x] 在 `4.3.2` 里补清 prompt layering：system prompt、business guidance、TRACE_CONTEXT 与结构化校验边界
- [x] 产出新的第 4.1-4.3 节修订版 `.docx`，并先做结构级自检
- [x] 为第 4.4 节补“图 + 代码框 + 压缩文字”证据版内容
- [x] 为 `4.4.1 / 4.4.2 / 4.4.3 / 4.4.4` 生成稳定的 PNG 机制图
- [x] 在 `4.3.2` 里补一个精简版 system prompt 骨架代码框，强化 prompt 固定规则证据
- [x] 产出新的第 4.1-4.4 节修订版 `.docx`，并先做结构级自检
- [x] 把 `4.4.1-4.4.4` 的 Mermaid 图改成中文主导版本，并重新导出 PNG
- [x] 把 `paper_4.5` 中用于正文的页面截图转成真正的 PNG，避免继续把 JPEG 伪装成 PNG 塞进 docx
- [x] 为 `4.5.1-4.5.4` 补“短文字 + 页面截图 + 代码框”证据版内容
- [x] 产出新的第 4.1-4.5 节修订版 `.docx`，并先做结构级自检
- [x] 为 `4.5.4` 补“基础设置”截图，并让设置页功能证据完整覆盖基础/AI/Prompt/Webhook/内核五块
- [x] 把 `4.5.1-4.5.4` 的文字从“页面职责”改成“具体功能点 + 对应接口/模块 + 页面证据”
- [x] 对当前清洁版论文全文做一次“写作旁白/内部心里话”排查，并清理残留句式
- [x] 把 `3.2` 改成贴合系统总体架构图的真实模块分组说明
- [x] 产出包含 `3.2-3.3` 收实结果与 `4.1-4.5` 图文代码证据的论文终检版 `.docx`
- [x] 把 `3.3.1-3.3.5` 改成更贴近真实模块、线程、队列和状态流转的流程说明
- [x] 产出包含 `3.3` 收实结果的新的论文修订版 `.docx`，并做结构级自检
- [x] 为 `3.2` 系统总体架构图补 Mermaid 源文件并导出 PNG
- [x] 为 `3.3` 系统主业务流程图补 Mermaid 源文件并导出 PNG
- [x] 仅替换论文中图 `3.1 / 3.2` 对应的 media 资源，避免重写 Word 正文结构
- [x] 修正 `3.1 / 3.2` Mermaid 替图时误伤封面图块的问题
- [x] 把 `3.1 / 3.2` 两张总图改成英文关键词 + 中文括号说明的双语标注
- [x] 提升 `3.1 / 3.2` 两张总图的导出分辨率，并重新生成论文修正版 `.docx`
- [x] 把 `4.3.1` AI 代理链路图改成中文主述版本，减少源码函数名直出造成的英文堆积
- [x] 把 `4.3.2` Prompt 分层图改成中文主述版本，保留必要英文术语但补足中文解释
- [x] 把 `4.2.1` 双缓冲写入图改成中文主述版本，并修正“未满桶”分支的中性表述
- [x] 把 `4.2.2` SQLite 刷盘图改成中文主述版本，减少事务函数名直出造成的源码味
- [x] 把 `4.2.3` 查询支撑图改成中文主述版本，保留接口与仓储名但补足中文解释
- [x] 把 `4.3.3` AI 可靠性图改成中文主述版本，保留状态码锚点但先说清状态含义
- [x] 把 `4.1.2` 会话生命周期图改成中文主述版本，状态名保留为英文锚点
- [x] 把 `4.1.3` 异步分发保护图改成中文主述版本，减少队列/墓碑/回滚路径中的源码味
- [x] 仅替换当前最新版论文中 `fig_4_2` 到 `fig_4_9` 对应的 media 资源，把收实后的第 4 章流程图真正并回 docx
- [x] 把 `fig_4_2` 到 `fig_4_9` 重编码为无额外 PNG 元数据的干净版本，并重新替回 docx，排查 Word 弹窗是否由图片编码触发
- [x] 从最后一个稳定版重新构建论文：只按原句命中替换少量正文段落块，并叠加清洁版 `fig_4_2` 到 `fig_4_9`，避免再次整树重写 `document.xml`
- [x] 继续收口第一章与第六章的高风险总述段，让读者更快看懂“系统做了什么、后续准备做什么”
- [x] 对当前安全版论文再做一轮全篇扫雷，只收主文里残留的空结尾、总结腔和不够直接的工程表述
- [x] 清掉第五章残留的写作旁白段，并把实验边界说明改成正文语气
- [x] 再做一次结构级自检，确认新导出的扫雷版没有重新触发 Word 兼容风险
- [x] 扫描当前扫雷版正文中被压平的文献引用，并恢复为模板要求的右上角上标格式
- [x] 对修复后的文档做结构级自检，并复核正文平排引用只剩参考文献列表与代码片段
- [x] 基于老师修订版清理第五章与附录中的作者旁白句式，保留事实边界但去掉“写作指导腔”
- [x] 对老师修订版清理后的新文档做结构级自检，并复核没有重新引入引用格式问题

## 2026-04-22 定稿格式检测跟踪

- [x] 以 `凌乐天-毕业论文-20260422_旁白清理版_格式修正10.docx` 作为当前格式修正基线
- [x] 复核 `定稿格式检测/32201175_凌乐天_定稿检测文件_格式统计报告.html` 仍是旧报告，顶部统计仍停留在 `一般错误 227 / 严重错误 0`
- [x] 复核第三批代码框在 `格式修正10` 中已经清干净：`17` 个代码框段落均为居中、无缩进，包内已无 `Consolas / 等线`
- [x] 清理 `fontTable.xml` 中已无正文引用的 `Consolas / 等线` 字体注册残影，并生成 `格式修正10`
- [x] 对 `格式修正10` 做结构级自检：`zipfile.testzip() == None`，压缩包文件数保持 `78`
- [ ] 重新按“最新文档真实状态”复核旧报告四类大项，不再直接相信旧报告逐条明细：
- [x] 全文字体/字号类：已先把附录 C 字段说明中的无样式散装段落统一挂到 `affa`，并确认第三批代码框与包内字体残影已清掉
- [x] 段落格式类：已先把附录 B 图表题注统一切到 `afe / afd`，附录 C 字段说明不再以左对齐无样式段落裸奔
- [ ] 内容书写规范类：重点复核英文关键词、三级标题序号空格、题注挂接是否还有漏项
- [ ] 参考文献类：重点复核 `4` 条旧报告问题在当前修正版中是否已经实质消失
- [ ] 单独判断 `styles.xml` 中仍保留的 `18` 号样式是否只是模板自带的 `Balloon Text / header / footer / 页眉字符`，避免误改模板合法样式
- [ ] 单独判断 `header5.xml / header8.xml` 中的 `两端对齐 + 首行缩进` 是否属于学校模板页眉固有格式，避免为了清检测器把页眉改坏
- [ ] 如果条件允许，让用户基于 `格式修正10` 重新跑一次定稿格式检测；新报告出来后再按“新报告优先”继续清理

- [x] 生成 `凌乐天-毕业论文-20260422_旁白清理版_格式修正11.docx`，仅做两类低风险格式收口：
- [x] 附录 B 图表题注从旧 `a6` 统一到 `afe / afd`
- [x] 附录 C `trace_id / span_id / risk_level` 等字段说明行统一挂到 `affa`
- [x] 对 `格式修正11` 做结构级自检：`zipfile.testzip() == None`，附录 B 已无残留 `a6` 题注
- [x] 生成 `凌乐天-毕业论文-20260422_旁白清理版_格式修正13.docx`，继续只做低风险文本级收口：
- [x] 目录中 `1.3.2 / 1.3.3 / 1.4.2 / 1.4.3 / 4.1.3` 的 `TOC3` 条目已补半角空格
- [x] 正文里 3 段源码味过重的 `::` 表述已改成职责化说法，不再直接写 `SpanEvent::trace_end / TraceSessionManager::Push / cpr::Session`
- [x] 对 `格式修正13` 做结构级自检：`zipfile.testzip() == None`，压缩包文件数仍为 `78`
- [x] 排查 `格式修正13` 再次触发 Word 弹窗的高疑似根因：目录 5 个三级标题节点带尾部空格，但缺少 `xml:space=\"preserve\"`
- [x] 生成 `凌乐天-毕业论文-20260422_旁白清理版_格式修正14.docx`，只对这 5 个 `w:t` 节点做最小字符串替换补 `xml:space=\"preserve\"`
- [x] 对 `格式修正14` 做结构级自检：`zipfile.testzip() == None`，并确认整份 `document.xml` 已无“前后带空格但缺少 xml:space”的节点
- [x] 复核附录 C 样式：JSON 示例段为 `HTML0`，说明标签为 `af1`，字段说明行为 `affa`，三者的 ASCII 字体均为 `Times New Roman`
- [x] 进一步确认弹窗高疑似根因不止是那 5 个目录空格节点，而是从 `格式修正11` 开始整树重写了 `word/document.xml`
- [x] 证据：`格式修正10` 与 `11/14` 的 `document.xml` 根前缀哈希不同；`10` 的根前缀长度为 `2622`，`11/14` 仅为 `846`
- [x] 生成 `凌乐天-毕业论文-20260422_旁白清理版_格式修正15.docx`，从 `格式修正10` 重新出发，只做原始字符串级局部补丁，不再整树重写 `document.xml`
- [x] 在 `格式修正15` 中安全补回四类修改：附录 B 题注样式、附录 C 字段说明样式、目录三级标题空格、正文去代码化表述
- [x] 对 `格式修正15` 做结构级自检：`zipfile.testzip() == None`，且 `document.xml` 根前缀哈希与 `格式修正10` 完全一致

### 你关心的几类问题当前状态

- [x] 三级标题格式：正文中的 `1.3.2 / 1.3.3 / 1.4.2 / 1.4.3 / 4.1.3` 已补半角空格
- [x] 三级标题目录项：目录里的对应 `TOC3` 条目也已补成和正文一致的写法
- [x] 图题、表题样式：正文与附录 B 的真实题注段落都已挂到 `afe / afd`
- [x] 代码框标题：`代码4.1-代码4.17` 已回到 `affa`，不再保留旧的居中/无缩进覆盖
- [x] 英文摘要关键词分隔：`Asynchronous Persistence; Lifecycle Protection` 已改对
- [x] 正文技术标识里的英文双冒号：正文说明段里残留的 `SpanEvent::trace_end / TraceSessionManager::Push / cpr::Session` 已全部改写
- [x] 参考文献补全：原先缺引用日期、缺出版地/出版者的那批条目已经补齐
- [x] 附录 C JSON / 接口说明字体：当前样式层面已经收口到 ASCII=`Times New Roman`、中文=`宋体`，暂未发现新的硬伤

## 2026-04-22 Word 弹窗引入点二分排查

- [x] 以 `格式修正5-格式修正10` 为窗口，逐个比对包内部件与 `document.xml`/关系文件哈希
- [x] 确认“哪一个版本开始出现非安全结构特征”，不要再靠记忆判断
- [x] 结合用户 Windows 实测锁定：`格式修正9` 无弹窗，`格式修正10` 开始弹窗
- [x] 包级对比确认 `9 -> 10` 只有 `word/fontTable.xml` 发生变化
- [x] 根因锁定：`格式修正10` 的 `fontTable.xml` 被重写后丢失了 `w14/w15/w16*` 等命名空间声明，但 `mc:Ignorable` 仍引用这些前缀，属于 Word 会修复的非法结构
- [x] 如果能锁定到单一 patch class，就从最后安全版重新最小重做该类改动
- [x] 生成 `凌乐天-毕业论文-20260422_旁白清理版_格式修正16.docx`：保留 `格式修正15` 的 `document.xml`，仅把 `fontTable.xml` 回退为 `格式修正9` 的安全版本

## 2026-04-22 格式修正16 对账旧检测报告

- [x] 提取旧检测报告中的主要错误类别与代表性问题
- [x] 对照 `格式修正16` 复核哪些已经实质修掉，哪些只是高概率已修但缺新报告确认
- [x] 列出剩余未收口项，按“可直接改 / 建议先重新检测”分组

## 2026-04-22 AIGC 修订版格式扫雷

- [x] 对 `凌乐天-毕业论文-20260422_格式修_aigc修.docx` 做包级体检：`zipfile.testzip() == None`，压缩包文件数仍为 `78`
- [x] 对比 `格式修正16` 与 `aigc修`：AIGC 编辑主要改动在 `document.xml` 正文文本，`styles/settings/header/footer` 的变化属于 Word 保存时的元数据刷新
- [x] 复核 XML 结构安全：整包 `xml/rels` 均可被解析，未发现新的 Word 兼容性硬错误
- [x] 复核正文改动段：题注样式数量与 `格式修正16` 一致，改动段中的正文引用仍为右上角上标
- [x] 复核复制粘贴高风险项：正文与样式层未重新引入 `Consolas / 等线 / Aptos` 等异常字体使用
- [x] 结论：当前 `aigc修` 未发现新的明显格式事故；主要变化是文字表达被重写，而不是模板样式被打散
